# Impact Advisory Collective — website

Single-page site for the Impact Advisory Collective. Static; renders client-side via the
Open Design `dc-runtime` (`support.js`).

> Pre-launch preview — carries `noindex` while legal entity, imprint and domain are confirmed.

- `index.html` — the page
- `support.js` — runtime
- `uploads/` — image assets
